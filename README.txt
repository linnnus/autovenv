Zsh plugin to automaticall activate/deactivate python virtual environments.

TODO: handle file system changes like creating a new venv or deleting one.

Try doing something like this:

```zsh
source autovenv.plugin.zsh
./create_examples.sh
cd example-basic/
cd ..
```
